name: Regenerate lockfile and verify npm ci

on:
  push:
    branches: [main]
    paths:
      - '.github/regenerate_lockfile.py'
      - '.github/workflows/regenerate-lockfile.yml'

permissions:
  contents: write

jobs:
  lockfile:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Regenerate package-lock
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund
      - name: Switch CI to npm ci
        run: python3 .github/regenerate_lockfile.py
      - name: Verify clean install
        run: npm ci --no-audit --no-fund
      - name: Verify build
        run: npm run build
      - name: Commit lockfile and CI
        run: |
          git config user.name "No Meu Bairro Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "chore: make dependency installs reproducible"
          git push origin HEAD:main
